import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'activity-callback-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
